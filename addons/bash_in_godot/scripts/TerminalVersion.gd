extends Object
class_name TerminalVersion

# I put this here because I know people
# who edit ConsoleNode.gd and don't want their changes
# to be overwritten every time there is an update
# because a single digit changed.
const VERSION = "0.2.4"
